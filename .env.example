# ── Bond Directory Environment Variables ─────────────────────────────────────
# Copy this file to .env and fill in values
# Do NOT commit .env to version control

PORT=5050
NODE_ENV=development

# ── Bond Directory Database ──────────────────────────────────────────────────
# MongoDB Atlas URI for the bondsdirectory database
BOND_MONGO_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/bondsdirectory
BOND_COLLECTION_NAME=bondsdirectory

# ── CORS ─────────────────────────────────────────────────────────────────────
# Comma-separated list of allowed origins. Leave unset to allow all (dev only).
# CORS_ORIGINS=https://gripinvest.in,https://staging.gripinvest.in

# ── NSDL Bond API Session Cookies ────────────────────────────────────────────
# Rotate every ~2-4 weeks when API starts returning 403.
# Get fresh values from: https://www.indiabondinfo.nsdl.com/CBDServices/
# DevTools → Network → any request → Cookie header → copy JSESSIONID + BIGipServerPool
NSDL_JSESSIONID=
NSDL_BIGipServerPool=

# ── Bond Sync Cron Schedule ──────────────────────────────────────────────────
# Default: every 12 days at 2 AM | https://crontab.guru/
BOND_SYNC_SCHEDULE=0 2 */12 * *
